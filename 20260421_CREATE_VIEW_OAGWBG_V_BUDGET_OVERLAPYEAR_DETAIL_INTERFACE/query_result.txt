=== Connection OK ===

=== VIEW DDL ===

  CREATE OR REPLACE FORCE EDITIONABLE VIEW "OAGWBG"."OAGWBG_V_BUDGET_OVERLAPYEAR_CENTRAL_DETAIL_INTERFACE" ("WEB_BATCH_NO", "USER_JE_SOURCE_NAME", "REFERENCE4", "REFERENCE5", "LEDGER_ID", "USER_JE_CATEGORY_NAME", "TRANSFER_DATE", "DEFAULT_EFFECTIVE_DATE", "ACTUAL_FLAG", "BUDGET_ENCUMBRANCE_NAME", "CURRENCY_CODE", "ATTRIBUTE3", "ATTRIBUTE4", "LINE_NUMBER", "SEGMENT1", "SEGMENT2", "SEGMENT3", "SEGMENT4", "SEGMENT5", "SEGMENT6", "SEGMENT7", "SEGMENT8", "SEGMENT9", "SEGMENT10", "SEGMENT11", "SEGMENT12", "SEGMENT13", "ENTERED_DR", "ACCOUNTED_DR", "ENTERED_CR", "ACCOUNTED_CR", "REFERENCE1", "CREATED_BY", "LAST_UPDATED_BY", "CREATION_DATE", "LAST_UPDATE_DATE", "REFERENCE10", "ENCUMBRANCE_TYPE", "TRANSFERNO", "RECEIPIENT_ACCOUNT", "SENDER_ACCOUNT", "TYPE", "TRANSFERTYPE") AS 
  WITH BASE AS (
  SELECT 
    BRSC.*,
    BRS.BUDGETYEAR AS BRS_BUDGETYEAR,
    BRS.DEPARTMENTID,
    BRS.COSTCENTERID,
    BRS.BUDGETSOURCEID,
    BRS.TRANSFERNO,
    EAR.BUDGETTYPEID,
    EAR.ACCOUNTNO,
    EAR.SUBACCOUNTNO,
    CC.NAME AS CATEGORY_NAME,
    SC.CONFIG_VALUE AS LEDGER_ID,
    BCC_GIVER.CASH_ACC AS GIVER,
    BCC_RECEIVER.CASH_ACC AS RECEIVER,
    BRS.TRANSFERDATE
  FROM OAGWBG_BUDGETRESERVED_CATEGORY BRSC
  LEFT JOIN OAGWBG_BUDGETRESERVED BRS
    ON BRSC.BUDGETRESERVEDID = BRS.ID
  LEFT JOIN OAGWBG_V_EXT_OAGPO_EXPENSE_ACCOUNT_RULE_V EAR
    ON BRSC.CATEGORYID = EAR.CATEGORYID
  LEFT JOIN OAGWBG_V_EXT_OAGINV_CATEGORY_CODES_V CC
    ON CC.ID = BRSC.CATEGORYID
  LEFT JOIN OAGWBG_BUDGETRESERVED_BANKACCOUNT BRB
  	ON BRB.RESERVEDID = BRS.ID 
  LEFT JOIN OAGWBG_V_EXT_OAGCE_BANK_ACCOUNT_V BCC_GIVER
  	ON BCC_GIVER.BANK_ACCOUNT_ID = BRB.BANKACCOUNTGIVER 
  LEFT JOIN OAGWBG_V_EXT_OAGCE_BANK_ACCOUNT_V BCC_RECEIVER
  	ON BCC_RECEIVER.BANK_ACCOUNT_ID = BRB.BANKACCOUNTRECEIVER  
  LEFT JOIN OAGWBG_SYSTEMCONFIG SC
  	ON SC.CONFIG_KEY = 'Ledger'
  WHERE BRS.ROUNDINTERFACE IS NOT NULL
),
TYPE_MAP AS (
  SELECT 'ENC' AS DOC_TYPE, 'E' AS ACTUAL_FLAG, 'C' AS DRCR_FLAG FROM DUAL
  UNION ALL
  SELECT 'BG', 'B', 'D' FROM DUAL
)

SELECT
  NVL(RBN.BATCHNAME,
  CASE 
    WHEN T.DOC_TYPE = 'ENC' THEN
      'ENC_CARRY_FORWARD_' || B.BUDGETYEAR || '_' ||
      TO_CHAR(NVL(B.TRANSFERDATE, SYSDATE),'YYMMDDHH24MISS')
    ELSE
      'BG_CARRY_FORWARD_' || B.BUDGETYEAR || '_' ||
      TO_CHAR(NVL(B.TRANSFERDATE, SYSDATE),'YYMMDDHH24MISS')
  END ) AS WEB_BATCH_NO,

  'Web Budget' AS USER_JE_SOURCE_NAME,
  'เน€เธเธดเธเธเธฑเธเน€เธซเธฅเธทเนเธญเธกเธเธต ' || B.BUDGETYEAR || '-' || B.DEPARTMENTID || ' เน€เธฅเธเธ—เธตเนเน€เธเธดเธเธเธฑเธ ' || B.TRANSFERNO AS REFERENCE4,
  'เน€เธเธดเธเธเธฑเธเน€เธซเธฅเธทเนเธญเธกเธเธต ' || B.BUDGETYEAR || '-' || B.DEPARTMENTID || ' เน€เธฅเธเธ—เธตเนเน€เธเธดเธเธเธฑเธ ' || B.TRANSFERNO AS REFERENCE5,
  B.LEDGER_ID,

  CASE
  	WHEN T.DOC_TYPE = 'ENC' THEN 'Web Encumbrance'
  	ELSE 'Budget - เน€เธเธดเธเธเธฑเธ'
  END AS USER_JE_CATEGORY_NAME,
  
  TO_CHAR(B.TRANSFERDATE,'YYYY-MM-DD') AS TRANSFER_DATE,

  CASE 
    WHEN T.DOC_TYPE = 'ENC' THEN (B.BRS_BUDGETYEAR - 543) || '-SEP-30'
    ELSE (B.BRS_BUDGETYEAR - 543) || '-OCT-01'
  END AS DEFAULT_EFFECTIVE_DATE,

  T.ACTUAL_FLAG,

  CASE 
    WHEN T.DOC_TYPE = 'BG' THEN 'OAG_BG_FINAL'
    ELSE ''
  END AS BUDGET_ENCUMBRANCE_NAME,

  'THB' AS CURRENCY_CODE,
  '' AS ATTRIBUTE3,
  B.TRANSFERNO AS ATTRIBUTE4,
  ROW_NUMBER() OVER (
  PARTITION BY B.BUDGETRESERVEDID, T.DOC_TYPE
  ORDER BY 
    B.CATEGORYID,
    B.BUDGETPLANID
  ) AS LINE_NUMBER,

  B.DEPARTMENTID AS SEGMENT1,
  B.COSTCENTERID AS SEGMENT2,
  MOD(B.BUDGETYEAR,100) AS SEGMENT3,

  CASE 
    WHEN T.DOC_TYPE = 'BG' THEN '400'
    ELSE B.BUDGETSOURCEID
  END AS SEGMENT4,

  LPAD(
  NVL(
    TO_CHAR(
      CASE 
        WHEN B.BUDGETPLANID = 90000 THEN 0
        ELSE B.BUDGETPLANID
      END
    ),
    '0')
     ,5,'0') AS SEGMENT5,

  LPAD(NVL(TO_CHAR(B.PRODUCTID),'0'),5,'0') AS SEGMENT6,
  B.ACTIVITYID AS SEGMENT7,
  B.BUDGETTYPEID AS SEGMENT8,
  NVL(B.BUDGETCODEID,'00000000000000000000') AS SEGMENT9,
  B.ACCOUNTNO AS SEGMENT10,
  B.SUBACCOUNTNO AS SEGMENT11,
  '00' AS SEGMENT12,
  '000' AS SEGMENT13,

  B.TOTALRESERVEDAMOUNT AS ENTERED_DR,
  B.TOTALRESERVEDAMOUNT AS ACCOUNTED_DR,
  NULL AS ENTERED_CR,
  NULL AS ACCOUNTED_CR,

  NVL(RBN.BATCHNAME,
  CASE 
    WHEN T.DOC_TYPE = 'ENC' THEN
      'ENC_CARRY_FORWARD_' || B.BUDGETYEAR || '_' ||
      TO_CHAR(NVL(B.TRANSFERDATE, SYSDATE),'YYMMDDHH24MISS')
    ELSE
      'BG_CARRY_FORWARD_' || B.BUDGETYEAR || '_' ||
      TO_CHAR(NVL(B.TRANSFERDATE, SYSDATE),'YYMMDDHH24MISS')
  END ) AS REFERENCE1,

  B.CREATEBY AS CREATED_BY,
  B.UPDATEBY AS LAST_UPDATED_BY,
  B.CREATEON AS CREATION_DATE,
  B.UPDATEON AS LAST_UPDATE_DATE,
  B.CATEGORY_NAME AS REFERENCE10,

  CASE WHEN T.DOC_TYPE = 'ENC' THEN 'Web Encumbrance' ELSE '' END AS ENCUMBRANCE_TYPE,
  B.TRANSFERNO, 
  CASE WHEN T.DOC_TYPE = 'ENC' THEN B.RECEIVER ELSE NULL END AS RECEIPIENT_ACCOUNT,
  CASE WHEN T.DOC_TYPE = 'ENC' THEN NULL ELSE B.GIVER END AS SENDER_ACCOUNT,

  CASE WHEN T.DOC_TYPE = 'ENC' THEN 'O' ELSE 'I' END AS TYPE,
  'CARRYFWD' AS TRANSFERTYPE

FROM BASE B
CROSS JOIN TYPE_MAP T
LEFT JOIN OAGWBG_BUDGETRECEIVE BR
  ON BR.BUDGETRESERVEDCATEGORYID = B.ID
LEFT JOIN OAGWBG_RECEIVE_BATCH_NO RBN
  ON RBN.RECEIVEID = BR.ID
 AND RBN.ROUNDNO = B.TRANSFERNO
 AND RBN.INTERFACETYPE = 'O'
 AND (
      (T.DOC_TYPE = 'ENC' AND RBN.BATCHNAME LIKE 'ENC%')
   OR (T.DOC_TYPE = 'BG' AND RBN.BATCHNAME LIKE 'BG%')
 )
ORDER BY B.TRANSFERNO, B.ID, T.ACTUAL_FLAG DESC

=== VIEW TEXT (chunks) ===
Text length: 4543
WITH BASE AS (
  SELECT 
    BRSC.*,
    BRS.BUDGETYEAR AS BRS_BUDGETYEAR,
    BRS.DEPARTMENTID,
    BRS.COSTCENTERID,
    BRS.BUDGETSOURCEID,
    BRS.TRANSFERNO,
    EAR.BUDGETTYPEID,
    EAR.ACCOUNTNO,
    EAR.SUBACCOUNTNO,
    CC.NAME AS CATEGORY_NAME,
    SC.CONFIG_VALUE AS LEDGER_ID,
    BCC_GIVER.CASH_ACC AS GIVER,
    BCC_RECEIVER.CASH_ACC AS RECEIVER,
    BRS.TRANSFERDATE
  FROM OAGWBG_BUDGETRESERVED_CATEGORY BRSC
  LEFT JOIN OAGWBG_BUDGETRESERVED BRS
    ON BRSC.BUDGETRESERVEDID = BRS.ID
  LEFT JOIN OAGWBG_V_EXT_OAGPO_EXPENSE_ACCOUNT_RULE_V EAR
    ON BRSC.CATEGORYID = EAR.CATEGORYID
  LEFT JOIN OAGWBG_V_EXT_OAGINV_CATEGORY_CODES_V CC
    ON CC.ID = BRSC.CATEGORYID
  LEFT JOIN OAGWBG_BUDGETRESERVED_BANKACCOUNT BRB
  	ON BRB.RESERVEDID = BRS.ID 
  LEFT JOIN OAGWBG_V_EXT_OAGCE_BANK_ACCOUNT_V BCC_GIVER
  	ON BCC_GIVER.BANK_ACCOUNT_ID = BRB.BANKACCOUNTGIVER 
  LEFT JOIN OAGWBG_V_EXT_OAGCE_BANK_ACCOUNT_V BCC_RECEIVER
  	ON BCC_RECEIVER.BANK_ACCOUNT_ID = BRB.BANKACCOUNTRECEIVER  
  LEFT JOIN OAGWBG_SYSTEMCONFIG SC
  	ON SC.CONFIG_KEY = 'Ledger'
  WHERE BRS.ROUNDINTERFACE IS NOT NULL
),
TYPE_MAP AS (
  SELECT 'ENC' AS DOC_TYPE, 'E' AS ACTUAL_FLAG, 'C' AS DRCR_FLAG FROM DUAL
  UNION ALL
  SELECT 'BG', 'B', 'D' FROM DUAL
)

SELECT
  NVL(RBN.BATCHNAME,
  CASE 
    WHEN T.DOC_TYPE = 'ENC' THEN
      'ENC_CARRY_FORWARD_' || B.BUDGETYEAR || '_' ||
      TO_CHAR(NVL(B.TRANSFERDATE, SYSDATE),'YYMMDDHH24MISS')
    ELSE
      'BG_CARRY_FORWARD_' || B.BUDGETYEAR || '_' ||
      TO_CHAR(NVL(B.TRANSFERDATE, SYSDATE),'YYMMDDHH24MISS')
  END ) AS WEB_BATCH_NO,

  'Web Budget' AS USER_JE_SOURCE_NAME,
  'เน€เธเธดเธเธเธฑเธเน€เธซเธฅเธทเนเธญเธกเธเธต ' || B.BUDGETYEAR || '-' || B.DEPARTMENTID || ' เน€เธฅเธเธ—เธตเนเน€เธเธดเธเธเธฑเธ ' || B.TRANSFERNO AS REFERENCE4,
  'เน€เธเธดเธเธเธฑเธเน€เธซเธฅเธทเนเธญเธกเธเธต ' || B.BUDGETYEAR || '-' || B.DEPARTMENTID || ' เน€เธฅเธเธ—เธตเนเน€เธเธดเธเธเธฑเธ ' || B.TRANSFERNO AS REFERENCE5,
  B.LEDGER_ID,

  CASE
  	WHEN T.DOC_TYPE = 'ENC' THEN 'Web Encumbrance'
  	ELSE 'Budget - เน€เธเธดเธเธเธฑเธ'
  END AS USER_JE_CATEGORY_NAME,
  
  TO_CHAR(B.TRANSFERDATE,'YYYY-MM-DD') AS TRANSFER_DATE,

  CASE 
    WHEN T.DOC_TYPE = 'ENC' THEN (B.BRS_BUDGETYEAR - 543) || '-SEP-30'
    ELSE (B.BRS_BUDGETYEAR - 543) || '-OCT-01'
  END AS DEFAULT_EFFECTIVE_DATE,

  T.ACTUAL_FLAG,

  CASE 
    WHEN T.DOC_TYPE = 'BG' THEN 'OAG_BG_FINAL'
    ELSE ''
  END AS BUDGET_ENCUMBRANCE_NAME,

  'THB' AS CURRENCY_CODE,
  '' AS ATTRIBUTE3,
  B.TRANSFERNO AS ATTRIBUTE4,
  ROW_NUMBER() OVER (
  PARTITION BY B.BUDGETRESERVEDID, T.DOC_TYPE
  ORDER BY 
    B.CATEGORYID,
    B.BUDGETPLANID
  ) AS LINE_NUMBER,

  B.DEPARTMENTID AS SEGMENT1,
  B.COSTCENTERID AS SEGMENT2,
  MOD(B.BUDGETYEAR,100) AS SEGMENT3,

  CASE 
    WHEN T.DOC_TYPE = 'BG' THEN '400'
    ELSE B.BUDGETSOURCEID
  END AS SEGMENT4,

  LPAD(
  NVL(
    TO_CHAR(
      CASE 
        WHEN B.BUDGETPLANID = 90000 THEN 0
        ELSE B.BUDGETPLANID
      END
    ),
    '0')
     ,5,'0') AS SEGMENT5,

  LPAD(NVL(TO_CHAR(B.PRODUCTID),'0'),5,'0') AS SEGMENT6,
  B.ACTIVITYID AS SEGMENT7,
  B.BUDGETTYPEID AS SEGMENT8,
  NVL(B.BUDGETCODEID,'00000000000000000000') AS SEGMENT9,
  B.ACCOUNTNO AS SEGMENT10,
  B.SUBACCOUNTNO AS SEGMENT11,
  '00' AS SEGMENT12,
  '000' AS SEGMENT13,

  B.TOTALRESERVEDAMOUNT AS ENTERED_DR,
  B.TOTALRESERVEDAMOUNT AS ACCOUNTED_DR,
  NULL AS ENTERED_CR,
  NULL AS ACCOUNTED_CR,

  NVL(RBN.BATCHNAME,
  CASE 
    WHEN T.DOC_TYPE = 'ENC' THEN
      'ENC_CARRY_FORWARD_' || B.BUDGETYEAR || '_' ||
      TO_CHAR(NVL(B.TRANSFERDATE, SYSDATE),'YYMMDDHH24MISS')
    ELSE
      'BG_CARRY_FORWARD_' || B.BUDGETYEAR || '_' ||
      TO_CHAR(NVL(B.TRANSFERDATE, SYSDATE),'YYMMDDHH24MISS')
  END ) AS REFERENCE1,

  B.CREATEBY AS CREATED_BY,
  B.UPDATEBY AS LAST_UPDATED_BY,
  B.CREATEON AS CREATION_DATE,
  B.UPDATEON AS LAST_UPDATE_DATE,
  B.CATEGORY_NAME AS REFERENCE10,

  CASE WHEN T.DOC_TYPE = 'ENC' THEN 'Web Encumbrance' ELSE '' END AS ENCUMBRANCE_TYPE,
  B.TRANSFERNO, 
  CASE WHEN T.DOC_TYPE = 'ENC' THEN B.RECEIVER ELSE NULL END AS RECEIPIENT_ACCOUNT,
  CASE WHEN T.DOC_TYPE = 'ENC' THEN NULL ELSE B.GIVER END AS SENDER_ACCOUNT,

  CASE WHEN T.DOC_TYPE = 'ENC' THEN 'O' ELSE 'I' END AS TYPE,
  'CARRYFWD' AS TRANSFERTYPE

FROM BASE B
CROSS JOIN TYPE_MAP T
LEFT JOIN OAGWBG_BUDGETRECEIVE BR
  ON BR.BUDGETRESERVEDCATEGORYID = B.ID
LEFT JOIN OAGWBG_RECEIVE_BATCH_NO RBN
  ON RBN.RECEIVEID = BR.ID
 AND RBN.ROUNDNO = B.TRANSFERNO
 AND RBN.INTERFACETYPE = 'O'
 AND (
      (T.DOC_TYPE = 'ENC' AND RBN.BATCHNAME LIKE 'ENC%')
   OR (T.DOC_TYPE = 'BG' AND RBN.BATCHNAME LIKE 'BG%')
 )
ORDER BY B.TRANSFERNO, B.ID, T.ACTUAL_FLAG DESC

=== COLUMNS ===
1. WEB_BATCH_NO [VARCHAR2(255)] NULL=Y
2. USER_JE_SOURCE_NAME [CHAR(10)] NULL=Y
3. REFERENCE4 [VARCHAR2(478)] NULL=Y
4. REFERENCE5 [VARCHAR2(478)] NULL=Y
5. LEDGER_ID [VARCHAR2(4000)] NULL=Y
6. USER_JE_CATEGORY_NAME [VARCHAR2(16)] NULL=Y
7. TRANSFER_DATE [VARCHAR2(10)] NULL=Y
8. DEFAULT_EFFECTIVE_DATE [VARCHAR2(47)] NULL=Y
9. ACTUAL_FLAG [CHAR(1)] NULL=Y
10. BUDGET_ENCUMBRANCE_NAME [VARCHAR2(12)] NULL=Y
11. CURRENCY_CODE [CHAR(3)] NULL=Y
12. ATTRIBUTE3 [CHAR(0)] NULL=Y
13. ATTRIBUTE4 [VARCHAR2(255)] NULL=Y
14. LINE_NUMBER [NUMBER(22)] NULL=Y
15. SEGMENT1 [VARCHAR2(150)] NULL=Y
16. SEGMENT2 [VARCHAR2(150)] NULL=Y
17. SEGMENT3 [NUMBER(22)] NULL=Y
18. SEGMENT4 [VARCHAR2(255)] NULL=Y
19. SEGMENT5 [VARCHAR2(5)] NULL=Y
20. SEGMENT6 [VARCHAR2(5)] NULL=Y
21. SEGMENT7 [VARCHAR2(4000)] NULL=Y
22. SEGMENT8 [VARCHAR2(25)] NULL=Y
23. SEGMENT9 [VARCHAR2(150)] NULL=Y
24. SEGMENT10 [VARCHAR2(25)] NULL=Y
25. SEGMENT11 [VARCHAR2(25)] NULL=Y
26. SEGMENT12 [CHAR(2)] NULL=Y
27. SEGMENT13 [CHAR(3)] NULL=Y
28. ENTERED_DR [NUMBER(22)] NULL=Y
29. ACCOUNTED_DR [NUMBER(22)] NULL=Y
30. ENTERED_CR [VARCHAR2(0)] NULL=Y
31. ACCOUNTED_CR [VARCHAR2(0)] NULL=Y
32. REFERENCE1 [VARCHAR2(255)] NULL=Y
33. CREATED_BY [NUMBER(22)] NULL=Y
34. LAST_UPDATED_BY [NUMBER(22)] NULL=Y
35. CREATION_DATE [TIMESTAMP(6)(11)] NULL=Y
36. LAST_UPDATE_DATE [TIMESTAMP(6)(11)] NULL=Y
37. REFERENCE10 [VARCHAR2(240)] NULL=Y
38. ENCUMBRANCE_TYPE [VARCHAR2(15)] NULL=Y
39. TRANSFERNO [VARCHAR2(255)] NULL=Y
40. RECEIPIENT_ACCOUNT [VARCHAR2(337)] NULL=Y
41. SENDER_ACCOUNT [VARCHAR2(337)] NULL=Y
42. TYPE [CHAR(1)] NULL=Y
43. TRANSFERTYPE [CHAR(8)] NULL=Y

=== VIEW DEPENDENCIES (Tables/Views) ===
SYNONYM: PUBLIC.DUAL
TABLE: OAGWBG.OAGWBG_BUDGETRECEIVE
TABLE: OAGWBG.OAGWBG_BUDGETRESERVED
TABLE: OAGWBG.OAGWBG_BUDGETRESERVED_BANKACCOUNT
TABLE: OAGWBG.OAGWBG_BUDGETRESERVED_CATEGORY
TABLE: OAGWBG.OAGWBG_RECEIVE_BATCH_NO
TABLE: OAGWBG.OAGWBG_SYSTEMCONFIG
VIEW: OAGWBG.OAGWBG_V_EXT_OAGCE_BANK_ACCOUNT_V
VIEW: OAGWBG.OAGWBG_V_EXT_OAGINV_CATEGORY_CODES_V
VIEW: OAGWBG.OAGWBG_V_EXT_OAGPO_EXPENSE_ACCOUNT_RULE_V

=== Done ===
